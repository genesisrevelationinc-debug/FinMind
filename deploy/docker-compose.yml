version: '3.8'

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  backend:
    build:
      context: ../backend
      - "5000:5000"
    depends_on:
      - db
      - redis

volumes:
  redis-data: