apiVersion: v1
kind: Service
metadata:
  name: finmind-backend-service
spec:
  selector:
    app: finmind-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
  externalTrafficPolicy: Local