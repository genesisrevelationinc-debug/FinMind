openapi: 3.0.0
info:
  title: FinMind API
  version: 1.0.0
paths:
  /api/auth/register:
    post:
      summary: Register a new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string
                email:
                  type: string
                password:
                  type: string
      responses:
        '201':
          description: User created
  /api/auth/login:
    post:
      summary: Login a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: User logged in
  /api/dashboard/overview:
    get:
      summary: Get financial overview for the current user
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Financial overview
          content:
            application/json:
              schema:
                type: object
                properties:
                  total_expenses:
                    type: number
                  total_bills:
                    type: number
                  upcoming_bills:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        name:
                          type: string
                        amount:
                          type: number
                        due_date:
                          type: string
                        paid:
                          type: boolean
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT