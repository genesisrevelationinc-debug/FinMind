openapi: 3.0.0
info:
  title: FinMind API
  version: 1.0.0
    get:
      summary: Get user overview
      responses:
        '200':
          description: A JSON array of user data
  /multi-account/overview:
    get:
      summary: Get multi-account financial overview
      parameters:
        - in: query
          name: user_id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: A JSON array of account overviews